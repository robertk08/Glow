#pragma once
// Optional. Copy to secrets.h to skip provisioning on a bench node: used only
// when NVS is empty, never written to NVS. 2.4GHz WPA2-PSK only.

#define WIFI_SSID     "your-network"
#define WIFI_PASSWORD "your-password"
